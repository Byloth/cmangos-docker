name: WotLK build & release
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

jobs:
  wotlk-build-n-release:
    uses: ./.github/workflows/build-n-release.yml
    with:
      expansion: wotlk
