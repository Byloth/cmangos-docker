{
  // https://nuxt.com/docs/guide/concepts/typescript
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    // Tests run on Bun (`bun test`); Nuxt's generated tsconfig sets
    // "types": [], so bun's ambient types must be opted into explicitly.
    "types": ["bun-types"]
  }
}
